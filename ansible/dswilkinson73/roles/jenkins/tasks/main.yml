---
# tasks file for roles/jenkins
- name: install default-jre
  package:
    name: default-jre
    state: present
- name: install pip packages
  pip:
    name: virtualenv
    state: present
    executable: pip3
