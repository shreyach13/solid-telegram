apiVersion: v1
kind: ConfigMap
metadata:
  name: special-config
  namespace: green
data:
  SPECIAL_LEVEL: very
  SPECIAL_TYPE: charm
---
apiVersion: apps/v1
kind: Pod
metadata:
  name: green-vault-setup
  namespace: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vault
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: green-vault-setup
    spec:
      containers:
      - image: alpine
        name: green-vault-setup
        volumeMounts:
        - name: green-vault-volume
          mountPath: /mnt
        command: ["/bin/bash"]
        args:
          -c
          /mnt/configure_vault.sh 
        env:
        - name: VAULT_ADDR
          value: http://my_vault:8200

      volumes:
      - name: green-vault-volume
        configMap:
      restartPolicy: Never

apiVersion: v1
kind: Pod
metadata:
  name: dapi-test-pod
spec:
  containers:
    - name: test-container
      image: k8s.gcr.io/busybox
      command: [ "/bin/sh", "-c", "ls /etc/config/" ]
      volumeMounts:
      - name: config-volume
        mountPath: /etc/config
  volumes:
    - name: config-volume
      configMap:
        # Provide the name of the ConfigMap containing the files you want
        # to add to the container
        name: special-config
  restartPolicy: Never
